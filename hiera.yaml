---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "role/%{role}.yaml"
      - "nodes/%{hostname}.yaml"
      - "os/%{os.distro.id}.yaml"
      - "common.yaml"
